project Awlib is

   for Source_Dirs use ("src", "samples", "src-os");
   for Object_Dir use "lib";
   for Exec_Dir use "bin";
   for Main use ("fs_tests", "string_util_test.adb", "env_vars_test.adb");

   package Compiler is
      for Default_Switches ("ada") use ("-O2", "-gnat05");
   end Compiler;

   package Builder is
      for Executable ("string_util_test.adb") use "";
      for Executable ("env_vars_test.adb") use "";
   end Builder;

end Awlib;

